// PostCSS configuration for Tailwind in the desktop app.
// PostCSS configuration for Tailwind in the desktop app.
module.exports = {
  plugins: [
    require('postcss-nesting'),
    require('tailwindcss'),
    require('autoprefixer'),
  ],
};
